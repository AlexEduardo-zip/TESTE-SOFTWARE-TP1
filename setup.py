from setuptools import setup, find_packages

setup(
    name="tsbanking",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "textual",  # Adicione outras dependências se necessário
    ],
)
